# Frontend

Frontend application code belongs here.

Suggested structure:

- `src/components/` - reusable UI components
- `src/pages/` - route-level pages
- `src/services/` - API clients and external service helpers
- `src/types/` - shared TypeScript or data model types
